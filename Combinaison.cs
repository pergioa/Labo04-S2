using System;
using System.Text;

namespace lab04
{
    abstract class Combinaison
    {
        public Combinaison(int nbNombres, int min, int max)
        {
            // à faire
        }
        
        // classe à compléter

        public override string ToString()
        {
            // à faire
            return ""; // Instruction d'on le seul est est que ça compile 
        }
    }
}
